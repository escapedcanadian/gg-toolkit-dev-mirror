# Generated by make-ops.sh from the project's ops.yaml -- derived, not source.
ops/
# Local site definitions may carry addresses for a private lab.
lab.env.local
# Percentile decoder venv and captured metric streams (the streams are hundreds of MB).
hdrvenv/
*.jsonl
